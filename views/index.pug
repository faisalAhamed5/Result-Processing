extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action="/",method="POST")
    input(name="s_email",type="email",placeholder="Enter email address")
    button(type="submit") Submit